# B 题 模型口径与取舍备忘

## 1. 总体建模框架

四问共享同一个集合状态：每个频道的未知干扰源由一个位置可行集描述，在问题二中还带有接收半径的可行区间，在问题四中还带有发射朝向的可行集。三类观测对应三种集合运算：

| 检测结果 | 集合含义 |
| --- | --- |
| `direction`（示向度 $\hat\theta$，误差 $[-1^\circ,1^\circ]$） | 位置集与该方向的闭凸测向锥求交 |
| `near`（距离不超过 5 m） | 位置集与半径 5 m 的圆盘求交，是强正证据，可直接清除 |
| `no_signal` | 问题三中可由距离覆盖证书解释为"该频道不存在"；问题四中还含有"位于定向盲区"这一析取原因，不能单独作为结论 |

算法只在取得确定性证书时才把频道标记为 `cleared` 或 `absent_certified`。时间统一按

$$
T=\sum_k\frac{\lVert P_k-P_{k-1}\rVert_2}{5\ \mathrm{m/s}}
+(5\ \mathrm{s})N_{\text{measure}}+(1\ \mathrm{s})N_{\text{switch}}
+(3\ \mathrm{s})N_{\text{miss}}+(5\ \mathrm{s})N_{\text{success}}
$$

计算。清除指令的频道参数不改变测向机的当前频道；同一地点不做重复测量平均（题面规定该处误差固定）。

## 2. 题面模糊处的解释口径

以下四条是本模型对题面未明确之处的取舍，写作时按此表述：

1. **误差只有区间、没有分布。** 题面只给出误差落在 $[-1^\circ,1^\circ]$ 内，未给概率分布，故主结论一律采用确定性最坏情形；统计量只作敏感性对照，不能支撑保证性结论。
2. **"已经收到信号"给出了额外的信息。** 首次检测成功不仅说明源距 $\rho\le1500$ m，还说明该源的有效接收半径满足 $R_{\text{recv}}\ge\rho$。利用这一关系可得到更紧的候选区；忽略它只会导致过度保守。
3. **定向覆盖角包含边界。** 因而在发射半平面边界上仍能收到信号，问题四的"不可见"条件必须写成严格不等式，否则会给出错误的认证。
4. **圆域与半平面之交通常含圆弧。** 它不是严格的多边形，程序用内接与外接正多边形给出可核验的包围；论文中须区分真实凸集与离散逼近集合，不能让离散化偏差进入结论。

## 3. 失败场景与方案取舍

| 方案 | 确定性保证 | 主要收益 | 失败或代价 |
| --- | --- | --- | --- |
| 问题二 固定 1000 m 核心区 | 有 | 证明最简单 | 忽略首次检测给出的 $R_{\text{recv}}\ge\rho$，偏保守 |
| 问题二 条件接收区 | 有 | 候选区更完整 | 仍需处理圆域截断与连续最坏误差 |
| 问题二 贝叶斯熵减 | 依赖分布 | 可给平均意义最优 | 题面未给分布，不能支撑最坏保证 |
| 问题三 立即定位 | 有 | 状态逻辑直接 | 重复返回扫描骨架，移动代价高 |
| 问题三 两阶段批处理 | 有 | 不改变证书而减少往返 | 需做小规模在线顺序规划 |
| 问题四 少量多方位复测 | 无一般保证 | 平均动作少 | 可构造统一背向的漏源案例 |
| 问题四 31 点三角格 | 有 | 有限且证明短 | 扫描与移动开销较大，未证明点数全局最优 |
| 问题四 盲区后继续测向 | 无一般保证 | 成功时收缩快 | 边界朝向与半径下限下可一直无信号 |
| 问题四 扇形覆盖清除 | 有 | 与朝向无关 | 会产生"清除未发现"动作，最坏时间较大 |

## 4. 文献

- 赛题与接口规则：`problem/B题.pdf`、`problem/附件/附件1.docx`、`problem/附件/附件2.docx`。
- H. Jung, *Ueber die kleinste Kugel, die eine räumliche Figur einschliesst*, Journal für die reine und angewandte Mathematik, 123, 241–257, 1901, DOI: `10.1515/CRLL.1901.123.241`。
- G. T. Toussaint, *Solving Geometric Problems with the Rotating Calipers*, Proc. IEEE MELECON, A10.02/1–4, 1983。
