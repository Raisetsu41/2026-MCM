# 方案

要依次调用这些 Skill，按照其中要求完成任务。

用户偏好：

- 排版引擎：LaTeX
- 竞赛类型：高教社杯全国大学生数学建模竞赛（国赛）
- 论文语言：中文
- 子问题数量：题面共 4 个；本轮优先完整完成问题 1 与问题 2

## 本轮目标

1. 建立交会测向锥的半平面交模型，给出凸包与旋转卡壳直径算法。
2. 用荣格定理及严格反例判断“直径圆”覆盖命题。
3. 建立测向误差传播、GDOP 与第二检测点 Pareto 选择模型。
4. 导出第二检测点候选区域的解析边界。
5. 在仓库根目录实现 `geometry_solver.py`，运行单元测试并保存可复现结果和矢量图。

## workflow

| step | Skill | 本轮产物 |
| --- | --- | --- |
| 1 | `2analysis-modeling` | `reports/ANALYSIS_MODELING_REPORT.md` |
| 2 | `3coding-visual` | `geometry_solver.py`、`code/`、`tests/`、`results/`、`figures/`、`reports/RESULTS_REPORT.md` |
| 3 | `4drawio` | 后续按需绘制非数据型流程图，本轮不执行 |
| 4 | `5writing` | 后续使用 LaTeX 生成中文国赛论文，本轮不执行 |
| 5 | `6verity` | 完整论文完成后执行，本轮仅做代码与结果级核验 |

## 风险控制

- 角度统一在计算前转换为弧度，并处理跨越 $0^\circ$ 的示向区间。
- 将 $[-1^\circ,1^\circ]$ 同时解释为确定性误差界和均匀误差模型，避免混淆置信区间与硬约束。
- 目标圆域与半平面交并非严格多边形；代码采用外切正多边形近似并显式控制几何误差。
- 第二检测点只有单次示向度时缺少距离信息，必须报告候选源距离区间并采用稳健或分位数准则。
- 多目标优化不直接相加有量纲指标，输出 Pareto 非支配解或使用有明确阈值的 $\varepsilon$-约束。
- 旋转卡壳、半平面裁剪、Jacobian、GDOP 分别以解析例、暴力枚举和有限差分交叉验证。

