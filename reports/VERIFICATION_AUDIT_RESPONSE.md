# B 题红队报告回应与正式测试前决策

## 1. 技术判断

红队报告的核心判断是正确的：早期资产的主要风险不是论文表达，而是“一个数值程序在默认尺度上能跑，但其容差、保证集和终止证书没有共用同一数学语义”。平移后求交失效、把有限采样极值当严格上界、将首次测向退化为名义射线，都是会改变答案正确性的死穴。Q3/Q4 缺乏“不存在证书”也不是程序细节，而是终止逻辑未完成。

下列建议值得直接吸收：

- 用紧凸可行集统一判空、直径与最小覆盖圆，并用内外包围明确离散误差方向。
- 用解析连续域条件审查 Q2 候选点，采样只用于排序和近似误差指标。
- 将 Q3 的发现开销与定位开销拆开，先取得覆盖证书再批量定位，优先消除往返移动。
- 将 Q4 的“多方位观测”提升为局部凸包充要条件，不再把经验复测次数误当保证。

下列方向不适合作为正式测试前的主线：

- 把圆精确保留为 SOC 对统一理论表达有价值，但直径是单位圆方向上支撑宽度的最大值，并不因 SOC 表达自动变成一个简单凸优化。在线仍用多边形外包围更稳妥。
- 对所有 Q2 网格点做全区间算术代价过高。更合理的层级是解析条件筛选，有限采样排序，只对最终候选做区间分支定界。
- 题面未给误差分布，因此贝叶斯后验熵减和 POMDP 可作敏感性对照，不能承担确定性保证。完整 POMDP 也不符合正式测试前的实现风险。
- 证明 31 个 Q4 点对所有有限点集全局最优，或证明在线 TSP 的强竞争比，对当前得分帮助远小于一个可执行的完备闭环。

## 2. 缺陷清单与补丁

| ID | 文件与位置 | 问题 | 最小复现 | 补丁与验证 |
| --- | --- | --- | --- | --- |
| G-01 | `geometry_solver.py:47,238` | 凸包面积容差和卡壳距离平方比较共用绝对 `1e-10`，小尺度多边形丢顶点并给错直径 | $10^{-3}$ 量级四点的卡壳值与暴力最远点不等 | 先去平移，面积容差按 `span^2` 缩放，距离平方另用同量纲容差；三种尺度随机对拍通过 |
| G-02 | `geometry_solver.py:363` | Jacobian 的重合阈值随全局坐标放大，不具平移不变性 | 相对位移 $10^{-9}$ m 在原点可算，整体平移 $2\times10^6$ m 后被拒绝 | 只用已表示的相对向量判重合；$10^{-3}$ m 可表示相对位移在两个坐标量级上都返回有限值 |
| G-03 | `geometry_solver.py:89,119,161,178` | NaN/Inf 可进入三角函数或半平面并静默传播 | `bearing_halfplanes((0,0), NaN)` 返回全 NaN | 对角度、点、半平面、容差做有限性检查；非有限回归全部显式抛错 |
| G-04 | `geometry_solver.py:203` | 内外包围直径可因 $10^{-13}$ m 浮点噪声出现 `lower > upper` | 当前 Q1 算例原始差约 $2.4\times10^{-13}$ m | 差超过相对容差则视为程序错误，容差内向保守方向并拢 |
| Q2-01 | `geometry_solver.py:780` | Pareto wrapper 未将 `err_deg` 传给误差传播，非 1° 情形误差界仍按 1° | 设 `err_deg=2`，wrapper 值只有直接调用的一半 | 显式传参；2° wrapper 与直接调用完全一致 |
| Q2-02 | `geometry_solver.py:625,780` | 有限 `source_cone_samples` 覆盖率被当成连续扇形保证，角度格点之间可漏点 | 候选点 $(-500.01,0)$、接收界 2000 m、仅采扇形两个角端点时采样覆盖率为 1，但中心角最远距离 2000.01 m | wrapper 必须通过 `sector_max_distance` 的解析上包络；回归确认该点 `guaranteed=False` 且不进 Pareto |
| Q2-03 | `geometry_solver.py:660` | `sqrt(1-cos^2)` 在近平行时灾难消减，且用绝对行列式阈值判退化 | 夹角趋近 0 时 `cos` 先四舍五入成 1 | 用归一化二维叉积直接算 $|\sin\gamma|$，独立命名 `sin_angle_tol`；信息矩阵秩用 `rank_rel_tol` |
| R-01 | `code/outputs/*.jsonl`, `robot/main.py:25` | 历史指令日志明文写入身份字段，与提交约定直接冲突 | 搜索旧 JSONL 的 `robot_id` 字段 | 10 份日志已原位替换为 `<redacted>`；新日志对请求和响应递归脱敏，并有回归测试 |
| R-02 | `robot/main.py:86` | agent 异常路径已调用 `/exit`，入口又调一次，产生重复业务请求 | 任意 agent 异常都进入两层退出逻辑 | 保留 agent 内的单一退出责任，入口只记录错误 |
| R-03 | `robot/client.py:28` | 防护时间 2 s 小于默认请求超时 3 s，且客户端缺少供直接 agent/Mock 使用的 `enter()` | 在 deadline 前 2.5 s 发起一个 3 s 请求；`Q3Agent(client).run()` 调用不存在方法 | 每次请求超时动态取剩余安全时间与默认超时的较小值；补入幂等 `enter()` |
| R-04 | `robot/agent.py:123` | `near` 后默认认证清除曾被怀疑语义错误 | 源距测站 3 m 时测向返回 `near` | 不修改。`near` 是 $\le5$ m 强正证据，而 `/clear` 成功半径是 20 m 且与朝向无关；若失败应当报协议或状态故障 |
| R-05 | `robot/client.py`, `robot/agent.py:80` | 客户端与 agent 同时维护位姿的怀疑与当前代码不符 | 检查 `ApiClient` 实例不存在 `pos/current_channel` | 不修改。位置和当前测向频道只由 agent 维护 |
| R-06 | `main.tex:64` | AI 声明宣称方法和代码全部人工完成，与当前实际辅助范围不符 | 直接阅读声明 | 改为如实披露思路比较、代码辅助、审查测试与排版用途，并明确人工复核责任 |

## 3. 问题一、二的最终取舍

Q1 以

$$
K(\delta)=B_{1800}\cap\bigcap_iW_i(\delta)
$$

作为唯一几何对象。在线计算用 $K_N^-\subseteq K\subseteq K_N^+$ 给出可核验的直径包围，支撑函数

$$
D(K)=\max_{\|u\|=1}\{h_K(u)+h_K(-u)\}
$$

用于解释直径方向，不另造一套一维优化代码。$K(\delta)$ 随 $\delta$ 单调扩大，因而直径不减；它在活动集不变时分段光滑，但没有全局线性公式。直径圆可覆盖的正向充要条件是 $R^*(K)=D(K)/2$；对指定端点 $p,q$，等价于所有 $x\in K$ 满足 $(x-p)^T(x-q)\le0$。

Q2 不使用主观贝叶斯分布。主候选区改为条件于首次成功检测的

$$
\mathcal C_{\rm det}=\{X:\|X-G\|\le\max(1000,\|G-S_1\|),\ \forall G\in U_1\},
$$

并保留 $\mathcal C_{1000}$ 作为更保守的核心区。验证网格中，两者分别含 12093 和 4464 个点；当前字典序最优点仍位于核心区，所以坐标与旧结果一致。最坏误差目标 $f_1$ 在退化构型上定义为 $+\infty$；有限源采样得到的 60.8760 m 是稳健代理指标的数值近似，不再声称为连续域严格上界。

## 4. Q3 调度提速与官方验证

官方模拟器共完成 25 局有效 Q3 演练，清除数全部等于真值，清除失败次数合计为 0。基线（发现即定位）18 局的表现是：平均每源时间 507.913 s，中位数 490.536 s，最坏 628.701 s，标准差 49.482 s；虚拟时间中移动占 89.54%。这说明时间优化应投在路径上，而不是已经很快的几何计算上。

调度改动保留原点加半径 1200 m 正六边形的七点证书，只改变动作顺序：首次正检测改为入队，发现阶段走完后再按"距下一个安全侧偏点最近"滚动定位；若已发现 16 个不同频道，则用题面源数上界立即认证其余频道不存在。动作集合与证书均不变。

**官方 A/B 实测**（旧 18 局 vs 新 7 局）：

| 指标 | 立即定位（18 局） | 批处理（7 局） | 变化 |
| --- | ---: | ---: | ---: |
| 每源平均时间 | 507.91 s | **418.52 s** | **−89.39 s（−17.60%）** |
| 中位数 | 490.54 s | 443.90 s | −46.63 s |
| 最坏 | 628.70 s | 463.41 s | −165.29 s |
| 最好 | 455.99 s | 320.51 s | −135.48 s |
| 虚拟总时间均值 | 6598.0 s | 5485.7 s | −1112.3 s |
| 检测次数均值 | 106.9 | 100.7 | −6.2 |
| 清除失败合计 | 0 | 0 | 持平 |
| 清除达标 | 18/18 | 7/7 | 持平 |

两批面对的源数分布不同（前者 10–16，后者 11–16），按源数分层核验后优势仍成立：

| 干扰源数 | 立即定位每源时间 /s | 批处理每源时间 /s |
| --- | --- | --- |
| 11 | 628.70 | 444.60、443.97 |
| 13 | 465.01、535.09、477.64、502.89、474.52 | 436.60、443.90、463.42 |
| 16 | 464.74、475.39 | 320.51、376.66 |

13 源档两版区间不重叠（465.01–535.09 对 436.60–463.42），说明降幅不是源数分布造成的。

**降幅归因**：在相同 10 个随机案例上做配对压测，移动里程由 28.8 km 降至 24.0 km（−16.9%），同一批案例每源时间降幅 14.5%，官方两版实测降幅 17.60%。三者接近，说明时间降幅基本等于里程降幅，检测与清除的固定开销未变。检测次数由 106.9 降到 100.7 的差异更可能来自两批案例的源数分布，不计入调度收益。

**适用条件**：该优化仅在"一局内存在多个待定位源且与骨架点邻近"时成立；源越多越集中，收益越大（16 源局约 −31%，11 源局约 −29%）。

逐局明细见 `results/q3_schedule_speedup.csv`。

## 5. Q4 架构与保证

对可能源位置 $G$，记 $V_G=\{P_j-G:\|P_j-G\|\le1000\}$。未知朝向的闭半平面必定至少看到一个扫描点，当且仅当

$$
0\in\operatorname{conv}V_G,
$$

即 $G\in\operatorname{conv}\{P_j:\|P_j-G\|\le1000\}$。这是与格型无关的最弱确定性充要条件。若失败，严格分离向量和接收半径 1000 m 立即构造全部 `no_signal` 的反例。

代码取边长 990 m 的原点相位三角格，截取 $\|P\|\le2790$ m 得 31 点。任意 $G\in B_{1800}$ 都位于某个基本三角形，三顶点距离均不超过 990 m 且 $G$ 是它们的凸组合。31 点路由经最近邻加 2-opt 从 49764 m 降至 30424.73 m。

首次正测向后仍先尝试 Q3 的异地交会。后续 `no_signal` 在 Q4 不是故障，而是切换到首次测向扇形的三角覆盖清除。格点覆盖半径留有 0.5% 数值余量，301 个距离层与 41 个角层上的实测最大距离 19.8052 m。因 `/clear` 与朝向无关，该路径有限步内必然成功。

**官方演练 6 局**（定向源占比 1/12 至 14/16）：清除数全部等于真值，平均每源时间 845.8 s，最好 660.7 s，最坏 1096.9 s；检测次数 229–391 次（均值 289.8）；程序运行时间 2010–3955 ms。与 Q3 相比，平均时间约 1.7–2 倍、检测次数约 3 倍，原因同源：扫描点由 7 个增至 31 个，且 6 局合计 1518 次检测返回无信号（占全部检测的 87%），需靠覆盖式清除补齐。现实程序运行时间距 1200 s 上限仍有两个数量级以上余量。

这一组数字说明当前 Q4 是"可证完备但偏保守"的版本；空清除次数与 31 点移动是下一轮优化方向，不得通过削弱不存在证书来换取。

## 6. 未闭合风险与正式演练门禁

| 等级 | 风险 | 影响 | 门禁 |
| --- | --- | --- | --- |
| P0 | 新 Q3 batch 和 Q4 尚未在官方 GUI 演练 | Mock 无法完全复现官方服务语义 | 正式测试前由用户手动开窗演练。**已完成**：Q3 batch 已演练 7 局（见第 4 节），Q4 已演练 6 局，两问逐局核对真值、清除数、失败数与虚拟时间均通过 |
| P1 | Q4 完备版的虚拟时间和空清除数高 | 得分可能远低于启发式版 | 先保留作为安全版；官方演练记录定向源数与空清除，再评估是否加入带证书的扫描/清除交错调度 |
| P1 | Q2 的 60.8760 m 仍来自有限源采样 | 不能写成连续域严格 supremum | 论文标注“数值近似”；若要作严格上界，只对最终候选做区间分支定界 |
| P1 | 官方现实时间限制与接口拒绝语义只能由官方程序验证 | 正式 3 次机会可被协议差异消耗 | 不在正式局首次验证新逻辑；先用 practice 演练检查 `accepted`、幂等重试、唯一 `/exit` |
| P2 | Q1 精确圆域含圆弧，当前仍是 720/1440 边形包围 | 不能把一般结果叫“精确多边形” | 同时报 $D_{\rm low},D_{\rm up}$；只有间隙在显示精度下可忽略时才合并展示 |
| P2 | 31 点三角格未证明在所有有限点集中最少 | 不能声称全局最优点数 | 只声称“具有充要判据的可证有限构造”和“在规则三角剖分中高效” |
| P2 | 加密 Mock 不等于全连续证明 | 随机测试只能抓实现错误 | 论文中以凸包/三角剖分证明为主，随机压测仅作代码一致性证据 |

截止时间依题面为北京时间 2026-09-13 17:30，且 15:30 后不建议新建测试。因此正式演练顺序应是：先 Q3 batch 确认无回归，再 Q4 完备版，最后才做平均时间的启发式优化。任一局出现漏源、认证清除失败或重复退出，立即停止扩大演练并保留基线分支。

